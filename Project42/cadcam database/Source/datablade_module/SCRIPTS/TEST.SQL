-- Generated by BladeSmith 3.40.TC1, DO NOT MODIFY --

execute procedure ifx_allow_newline('t');

begin work;

create opaque type Loc(
	internallength = 8,
	alignment = 4
);

grant usage on type Loc to public;

create function LocIn (lvarchar)
returns Loc
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocInput)"
language c;

grant execute on function LocIn (lvarchar) to public;

create implicit cast 
(
	lvarchar as Loc
	with  LocIn
);

create function LocOut (Loc)
returns lvarchar
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocOutput)"
language c;

grant execute on function LocOut (Loc) to public;

create cast 
(
	Loc as lvarchar
	with  LocOut
);

create function LocSend (Loc)
returns sendrecv
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocSend)"
language c;

grant execute on function LocSend (Loc) to public;

create cast 
(
	Loc as sendrecv
	with  LocSend
);

create function LocRecv (sendrecv)
returns Loc
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocReceive)"
language c;

grant execute on function LocRecv (sendrecv) to public;

create implicit cast 
(
	sendrecv as Loc
	with  LocRecv
);

create function LocImpT (impexp)
returns Loc
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocImportText)"
language c;

grant execute on function LocImpT (impexp) to public;

create implicit cast 
(
	impexp as Loc
	with  LocImpT
);

create function LocExpT (Loc)
returns impexp
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocExportText)"
language c;

grant execute on function LocExpT (Loc) to public;

create cast 
(
	Loc as impexp
	with  LocExpT
);

create function LocImpB (impexpbin)
returns Loc
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocImportBinary)"
language c;

grant execute on function LocImpB (impexpbin) to public;

create implicit cast 
(
	impexpbin as Loc
	with  LocImpB
);

create function LocExpB (Loc)
returns impexpbin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocExportBinary)"
language c;

grant execute on function LocExpB (Loc) to public;

create cast 
(
	Loc as impexpbin
	with  LocExpB
);

create function Compare (Loc,Loc)
returns integer
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocCompare)"
language c;

grant execute on function Compare (Loc,Loc) to public;

create function Equal (Loc,Loc)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocEqual)"
language c;

grant execute on function Equal (Loc,Loc) to public;

create function NotEqual (Loc,Loc)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(LocNotEqual)"
language c;

grant execute on function NotEqual (Loc,Loc) to public;

create opaque type Component(
	internallength = 16,
	alignment = 4
);

grant usage on type Component to public;

create function ComponentIn (lvarchar)
returns Component
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentInput)"
language c;

grant execute on function ComponentIn (lvarchar) to public;

create implicit cast 
(
	lvarchar as Component
	with  ComponentIn
);

create function ComponentOut (Component)
returns lvarchar
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentOutput)"
language c;

grant execute on function ComponentOut (Component) to public;

create cast 
(
	Component as lvarchar
	with  ComponentOut
);

create function ComponentSend (Component)
returns sendrecv
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentSend)"
language c;

grant execute on function ComponentSend (Component) to public;

create cast 
(
	Component as sendrecv
	with  ComponentSend
);

create function ComponentRecv (sendrecv)
returns Component
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentReceive)"
language c;

grant execute on function ComponentRecv (sendrecv) to public;

create implicit cast 
(
	sendrecv as Component
	with  ComponentRecv
);

create function ComponentImpT (impexp)
returns Component
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentImportText)"
language c;

grant execute on function ComponentImpT (impexp) to public;

create implicit cast 
(
	impexp as Component
	with  ComponentImpT
);

create function ComponentExpT (Component)
returns impexp
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentExportText)"
language c;

grant execute on function ComponentExpT (Component) to public;

create cast 
(
	Component as impexp
	with  ComponentExpT
);

create function ComponentImpB (impexpbin)
returns Component
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentImportBinary)"
language c;

grant execute on function ComponentImpB (impexpbin) to public;

create implicit cast 
(
	impexpbin as Component
	with  ComponentImpB
);

create function ComponentExpB (Component)
returns impexpbin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentExportBinary)"
language c;

grant execute on function ComponentExpB (Component) to public;

create cast 
(
	Component as impexpbin
	with  ComponentExpB
);

create function Compare (Component,Component)
returns integer
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentCompare)"
language c;

grant execute on function Compare (Component,Component) to public;

create function Equal (Component,Component)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentEqual)"
language c;

grant execute on function Equal (Component,Component) to public;

create function NotEqual (Component,Component)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentNotEqual)"
language c;

grant execute on function NotEqual (Component,Component) to public;

create function LessThan (Component,Component)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentLessThan)"
language c;

grant execute on function LessThan (Component,Component) to public;

create function GreaterThan (Component,Component)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentGreaterThan)"
language c;

grant execute on function GreaterThan (Component,Component) to public;

create function LessThanOrEqual (Component,Component)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentLessThanOrEqual)"
language c;

grant execute on function LessThanOrEqual (Component,Component) to public;

create function GreaterThanOrEqual (Component,Component)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(ComponentGreaterThanOrEqual)"
language c;

grant execute on function GreaterThanOrEqual (Component,Component) to public;

create opaque type Pin(
	internallength = 20,
	alignment = 4
);

grant usage on type Pin to public;

create function PinIn (lvarchar)
returns Pin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinInput)"
language c;

grant execute on function PinIn (lvarchar) to public;

create implicit cast 
(
	lvarchar as Pin
	with  PinIn
);

create function PinOut (Pin)
returns lvarchar
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinOutput)"
language c;

grant execute on function PinOut (Pin) to public;

create cast 
(
	Pin as lvarchar
	with  PinOut
);

create function PinSend (Pin)
returns sendrecv
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinSend)"
language c;

grant execute on function PinSend (Pin) to public;

create cast 
(
	Pin as sendrecv
	with  PinSend
);

create function PinRecv (sendrecv)
returns Pin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinReceive)"
language c;

grant execute on function PinRecv (sendrecv) to public;

create implicit cast 
(
	sendrecv as Pin
	with  PinRecv
);

create function PinImpT (impexp)
returns Pin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinImportText)"
language c;

grant execute on function PinImpT (impexp) to public;

create implicit cast 
(
	impexp as Pin
	with  PinImpT
);

create function PinExpT (Pin)
returns impexp
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinExportText)"
language c;

grant execute on function PinExpT (Pin) to public;

create cast 
(
	Pin as impexp
	with  PinExpT
);

create function PinImpB (impexpbin)
returns Pin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinImportBinary)"
language c;

grant execute on function PinImpB (impexpbin) to public;

create implicit cast 
(
	impexpbin as Pin
	with  PinImpB
);

create function PinExpB (Pin)
returns impexpbin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinExportBinary)"
language c;

grant execute on function PinExpB (Pin) to public;

create cast 
(
	Pin as impexpbin
	with  PinExpB
);

create function Compare (Pin,Pin)
returns integer
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinCompare)"
language c;

grant execute on function Compare (Pin,Pin) to public;

create function Equal (Pin,Pin)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinEqual)"
language c;

grant execute on function Equal (Pin,Pin) to public;

create function NotEqual (Pin,Pin)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinNotEqual)"
language c;

grant execute on function NotEqual (Pin,Pin) to public;

create function LessThan (Pin,Pin)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinLessThan)"
language c;

grant execute on function LessThan (Pin,Pin) to public;

create function GreaterThan (Pin,Pin)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinGreaterThan)"
language c;

grant execute on function GreaterThan (Pin,Pin) to public;

create function LessThanOrEqual (Pin,Pin)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinLessThanOrEqual)"
language c;

grant execute on function LessThanOrEqual (Pin,Pin) to public;

create function GreaterThanOrEqual (Pin,Pin)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinGreaterThanOrEqual)"
language c;

grant execute on function GreaterThanOrEqual (Pin,Pin) to public;

create opaque type Wire(
	internallength = 24,
	alignment = 4
);

grant usage on type Wire to public;

create function WireIn (lvarchar)
returns Wire
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireInput)"
language c;

grant execute on function WireIn (lvarchar) to public;

create implicit cast 
(
	lvarchar as Wire
	with  WireIn
);

create function WireOut (Wire)
returns lvarchar
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireOutput)"
language c;

grant execute on function WireOut (Wire) to public;

create cast 
(
	Wire as lvarchar
	with  WireOut
);

create function WireSend (Wire)
returns sendrecv
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireSend)"
language c;

grant execute on function WireSend (Wire) to public;

create cast 
(
	Wire as sendrecv
	with  WireSend
);

create function WireRecv (sendrecv)
returns Wire
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireReceive)"
language c;

grant execute on function WireRecv (sendrecv) to public;

create implicit cast 
(
	sendrecv as Wire
	with  WireRecv
);

create function WireImpT (impexp)
returns Wire
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireImportText)"
language c;

grant execute on function WireImpT (impexp) to public;

create implicit cast 
(
	impexp as Wire
	with  WireImpT
);

create function WireExpT (Wire)
returns impexp
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireExportText)"
language c;

grant execute on function WireExpT (Wire) to public;

create cast 
(
	Wire as impexp
	with  WireExpT
);

create function WireImpB (impexpbin)
returns Wire
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireImportBinary)"
language c;

grant execute on function WireImpB (impexpbin) to public;

create implicit cast 
(
	impexpbin as Wire
	with  WireImpB
);

create function WireExpB (Wire)
returns impexpbin
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireExportBinary)"
language c;

grant execute on function WireExpB (Wire) to public;

create cast 
(
	Wire as impexpbin
	with  WireExpB
);

create function Compare (Wire,Wire)
returns integer
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireCompare)"
language c;

grant execute on function Compare (Wire,Wire) to public;

create function Equal (Wire,Wire)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireEqual)"
language c;

grant execute on function Equal (Wire,Wire) to public;

create function NotEqual (Wire,Wire)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireNotEqual)"
language c;

grant execute on function NotEqual (Wire,Wire) to public;

create function LessThan (Wire,Wire)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireLessThan)"
language c;

grant execute on function LessThan (Wire,Wire) to public;

create function GreaterThan (Wire,Wire)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireGreaterThan)"
language c;

grant execute on function GreaterThan (Wire,Wire) to public;

create function LessThanOrEqual (Wire,Wire)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireLessThanOrEqual)"
language c;

grant execute on function LessThanOrEqual (Wire,Wire) to public;

create function GreaterThanOrEqual (Wire,Wire)
returns boolean
with
(
	not variant
)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(WireGreaterThanOrEqual)"
language c;

grant execute on function GreaterThanOrEqual (Wire,Wire) to public;

create function getid (Component)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compgetid)"
language c;

grant execute on function getid (Component) to public;

create function gettype (Component)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compgettype)"
language c;

grant execute on function gettype (Component) to public;

create function getxpos (Component)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compgetxpos)"
language c;

grant execute on function getxpos (Component) to public;

create function getypos (Component)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compgetypos)"
language c;

grant execute on function getypos (Component) to public;

create function getid (Pin)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pingetid)"
language c;

grant execute on function getid (Pin) to public;

create function getcomp (Pin)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pingetcomp)"
language c;

grant execute on function getcomp (Pin) to public;

create function getvalue (Pin)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pingetvalue)"
language c;

grant execute on function getvalue (Pin) to public;

create function checkterm (Pin)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pincheckterm)"
language c;

grant execute on function checkterm (Pin) to public;

create function getxpos (Loc)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(locgetxpos)"
language c;

grant execute on function getxpos (Loc) to public;

create function getypos (Loc)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(locgetypos)"
language c;

grant execute on function getypos (Loc) to public;

create function getid (Wire)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wiregetid)"
language c;

grant execute on function getid (Wire) to public;

create function getfirstcomp (Wire)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wiregetfirstcomp)"
language c;

grant execute on function getfirstcomp (Wire) to public;

create function getfirstpin (Wire)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wiregetfirstpin)"
language c;

grant execute on function getfirstpin (Wire) to public;

create function getseccomp (Wire)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wiregetseccomp)"
language c;

grant execute on function getseccomp (Wire) to public;

create function getsecpin (Wire)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wiregetsecpin)"
language c;

grant execute on function getsecpin (Wire) to public;

create function getvalue (Wire)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wiregetvalue)"
language c;

grant execute on function getvalue (Wire) to public;

create procedure Setall (Pin,integer,integer,integer,integer,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(PinSetall)"
language c;

grant execute on procedure Setall (Pin,integer,integer,integer,integer,integer) to public;

create procedure Setall (Component,integer,integer,integer,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compSetall)"
language c;

grant execute on procedure Setall (Component,integer,integer,integer,integer) to public;

create procedure Setid (Component,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compSetid)"
language c;

grant execute on procedure Setid (Component,integer) to public;

create procedure Settype (Component,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compSettype)"
language c;

grant execute on procedure Settype (Component,integer) to public;

create procedure Setxpos (Component,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compSetxpos)"
language c;

grant execute on procedure Setxpos (Component,integer) to public;

create procedure Setypos (Component,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(compSetypos)"
language c;

grant execute on procedure Setypos (Component,integer) to public;

create procedure Setid (Pin,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pinSetid)"
language c;

grant execute on procedure Setid (Pin,integer) to public;

create procedure Setcomp (Pin,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pinSetcomp)"
language c;

grant execute on procedure Setcomp (Pin,integer) to public;

create procedure Setvalue (Pin,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pinSetvalue)"
language c;

grant execute on procedure Setvalue (Pin,integer) to public;

create procedure Setterm (Pin,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pinSetterm)"
language c;

grant execute on procedure Setterm (Pin,integer) to public;

create procedure Setall (Wire,integer,integer,integer,integer,integer,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetall)"
language c;

grant execute on procedure Setall (Wire,integer,integer,integer,integer,integer,integer) to public;

create procedure Setid (Wire,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetid)"
language c;

grant execute on procedure Setid (Wire,integer) to public;

create procedure Setfirstcomp (Wire,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetfirstcomp)"
language c;

grant execute on procedure Setfirstcomp (Wire,integer) to public;

create procedure Setfirstpin (Wire,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetfirstpin)"
language c;

grant execute on procedure Setfirstpin (Wire,integer) to public;

create procedure Setseccomp (Wire,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetseccomp)"
language c;

grant execute on procedure Setseccomp (Wire,integer) to public;

create procedure Setsecpin (Wire,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetsecpin)"
language c;

grant execute on procedure Setsecpin (Wire,integer) to public;

create procedure Setvalue (Wire,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(wireSetvalue)"
language c;

grant execute on procedure Setvalue (Wire,integer) to public;

create function getio (Pin)
returns integer
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pingetio)"
language c;

grant execute on function getio (Pin) to public;

create procedure setio (Pin,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(pinsetio)"
language c;

grant execute on procedure setio (Pin,integer) to public;

create procedure Setxpos (Loc,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(locSetxpos)"
language c;

grant execute on procedure Setxpos (Loc,integer) to public;

create procedure Setypos (Loc,integer)
external name "$INFORMIXDIR/extend/dblogic.3.0/dblogic.bld(locSetypos)"
language c;

grant execute on procedure Setypos (Loc,integer) to public;

commit work;

