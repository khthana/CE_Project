(Capture Packet                          (     Packet  Capture  will  capture  all(packets that match  your filter settings(for  further  analysis. This  will  also(display  information  of network  during(capture time i.e. start time, time used,(total captured packets, total bytes,peak(packets per second  and time, peak bytes(per second and time,peak utilization,and(total lost packets.                     (                                        ('C' : Packet Capture  will  start  when (      this key is pressed.              (                                        ('S' : To stop capturing packet.         (                                        (Generate Packet                         (     Packet Generator    will   generate(packets  and send to  network.  You  can(specify  source  and destination address(                                        (of the packets generated, also size,data(in  those  packets.In Addition, You  can(specify  numbers  of  packets  and  time(delay between each packet.              (                                        (     If 'Continue' check box is checked,(the packet  will be generated  until you(press 'F' or click [Finished].          (