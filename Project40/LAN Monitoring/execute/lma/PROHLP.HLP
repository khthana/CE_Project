(Protocol In Use                         (    Protocol  In  Use     displays   the(distribution   of   protocol   used   in(Datalink layer, Network layer, Transport(layer, and  Upper layer. Graph  displays(percent of each  protocol in  that layer(per all protocols in that layer.        (      And the numbers of  packets  which(use that protocol are  displayed  in the(right.                                  (     Use  these  keys  to  switch  among(those modes                             (     'D' : To display protocols in      (           Datalink layer.              (     'N' : To display protocols in      (           Network layer.               (     'T' : To display protocols in      (           Transport layer.             (     'C' : To display protocols in      (           Upper TCP layer.             (