(Conversation                            (      Conversation displays  numbers of (      bytes and packets to and from each(      node  including  applications that(      each pair use.Display settings can(      be set to match your need.        (                                        ( 'M' :  To switch  the screen  to one of(        three mode                      (                                        (    [ Mode 1 ]source address,destination(     address,  start time, last time,   (     numbers of packets, number of bytes(                                        (    [ Mode 2 ]source address,destination(     address,     numbers of packets and(     bytes to and from each pair.       (                                        (    [ Mode 3 ]source address,destination(     address, protocol                  (                                        ( 'S' :  To  switch  between  sorting  by(        numbers  of  total  packets  and(        numbers of packets per second   (                                        ( 'U' :  To page up the screen           (                                        ( 'D' :  To page down the screen         (                                        ( 'T' :  To  switch  between  displaying (        numbers of total packets and    (        numbers of packets per second   (
